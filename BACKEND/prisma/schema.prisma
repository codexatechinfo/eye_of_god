generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model acomp_prazos {
  mes_ref_livro         String?   @db.VarChar
  data_da_leitura       DateTime? @db.Date
  hora_da_leitura       DateTime? @db.Time(6)
  unidade_consumidora   String?   @db.VarChar
  codigo_da_localidade  String?   @db.VarChar
  etapa                 String?   @db.VarChar
  livro                 String?   @db.VarChar
  status_releitura      String?   @db.VarChar
  nome_do_usuario       String?   @db.VarChar
  regional              String?   @db.VarChar
  cidade                String?   @db.VarChar
  distrito              String?   @db.VarChar
  prazo_leitura         DateTime? @db.Date
  prazo_releitura       String?   @db.VarChar
  dias_atraso           String?   @db.VarChar
  prazo_reg             String?   @db.VarChar
  atraso_prazo_reg      String?   @db.VarChar
  data_leitura_anterior DateTime? @db.Date
  id                    BigInt    @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model atestados {
  mes_ref            String? @db.VarChar(255)
  matricula          String? @db.VarChar(255)
  colaborador        String? @db.VarChar(255)
  cargo              String? @db.VarChar(255)
  base               String? @db.VarChar(255)
  data_afastamento   String? @db.VarChar(255)
  data_retorno       String? @db.VarChar(255)
  qtd_dias_afastado  String? @db.VarChar(255)
  CID                String? @db.VarChar(255)
  medico             String? @db.VarChar(255)
  CRM                String? @db.VarChar(255)
  afastado_INSS      String? @db.VarChar(255)
  motivo_afastamento String? @db.VarChar(255)
  id                 BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model ativos_inativos {
  situacao          String? @db.VarChar(255)
  matricula         String? @db.VarChar(255)
  colaborador       String? @db.VarChar(255)
  cargo             String? @db.VarChar(255)
  base              String? @db.VarChar(255)
  admissao          String? @db.VarChar(255)
  dias_45           String? @map("45_dias") @db.VarChar(255)
  dias_90           String? @map("90_dias") @db.VarChar(255)
  volta_afastamento String? @db.VarChar(255)
  demissao          String? @db.VarChar(255)
  observacao        String? @db.VarChar(255)
  data_atualizacao  String? @db.VarChar(255)
  id                BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model atrasos {
  mes_ref       String? @db.VarChar(255)
  processo      String? @db.VarChar(255)
  status        String? @db.VarChar(255)
  local         String? @db.VarChar(255)
  municipio     String? @db.VarChar(255)
  etapa         String? @db.VarChar(255)
  livro         String? @db.VarChar(255)
  uc            String? @db.VarChar(255)
  leiturista    String? @db.VarChar(255)
  cod           String? @db.VarChar(255)
  dt_prev_livro String? @db.VarChar(255)
  dt_env_livro  String? @db.VarChar(255)
  dt_ret_livro  String? @db.VarChar(255)
  dt_exec_uc    String? @db.VarChar(255)
  atr_livro     String? @db.VarChar(255)
  atr_uc        String? @db.VarChar(255)
  total_de_us   String? @db.VarChar(255)
  multa         String? @db.VarChar(255)
  regional      String? @db.VarChar(255)

  @@ignore
}

model atribuidas_im {
  tipo_ss                     String?
  subtipo_os                  String?
  mr                          String?
  numero_os                   String?
  los                         String?
  local                       String?
  livro                       String?
  etapa                       String?
  dt_rec_abertura             String?
  dt_prev_limite              String?
  numero_solicitacao          String?
  uc                          String?
  bairro                      String?
  releitura                   String?
  qtd_digitados_nao_digitados String?
  leiturista                  String?
  dt_import                   String?
  hr_import                   String?
  mes_ref                     String?
  id                          BigInt  @id @default(autoincrement())
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model base_justificativas {
  mes_ref       String? @db.VarChar(255)
  processo      String? @db.VarChar(255)
  status        String? @db.VarChar(255)
  tp_loc        String? @db.VarChar(255)
  contrato      String? @db.VarChar(255)
  reg           String? @db.VarChar(255)
  local         String? @db.VarChar(255)
  municipio     String? @db.VarChar(255)
  etapa         String? @db.VarChar(255)
  livro         String? @db.VarChar(255)
  uc            String? @db.VarChar(255)
  leiturista    String? @db.VarChar(255)
  cod           String? @db.VarChar(255)
  dta_prev_liv  String? @db.VarChar(255)
  dta_envio_liv String? @db.VarChar(255)
  dta_ret_livro String? @db.VarChar(255)
  dta_exec_uc   String? @db.VarChar(255)
  atr_liv       String? @db.VarChar(255)
  atr_uc        String? @db.VarChar(255)
  justificativa String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model base_justificativas_parcial {
  processo              String? @db.VarChar(255)
  livro                 String? @db.VarChar(255)
  ocorrencia            String? @db.VarChar(255)
  dt_solicitacao        String? @db.VarChar(255)
  status_parecer_gestor String? @db.VarChar(255)
  dt_hr_parecer         String? @db.VarChar(255)
  valor_total           String? @db.VarChar(255)
  ajuste_previo         String?
  parecer_gestor        String?
  justificativa         String?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model base_oficial_imp {
  mes_ref      String? @db.VarChar(255)
  municipio    String? @db.VarChar(255)
  etapa        String? @db.VarChar(255)
  livro        String? @db.VarChar(255)
  registronome String? @db.VarChar(255)
  tot__leit    String? @map("tot._leit") @db.VarChar(255)
  impto        String? @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 000 String? @map("000") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 094 String? @map("094") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 098 String? @map("098") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 099 String? @map("099") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 001 String? @map("001") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 002 String? @map("002") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 003 String? @map("003") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 004 String? @map("004") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 005 String? @map("005") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 006 String? @map("006") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 008 String? @map("008") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 009 String? @map("009") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 011 String? @map("011") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 012 String? @map("012") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 013 String? @map("013") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 016 String? @map("016") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 017 String? @map("017") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 018 String? @map("018") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 020 String? @map("020") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 021 String? @map("021") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 022 String? @map("022") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 027 String? @map("027") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 028 String? @map("028") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 029 String? @map("029") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 037 String? @map("037") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  inicio       String? @db.VarChar(255)
  fim          String? @db.VarChar(255)
  origem_dados String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model calendario_leitura {
  mes_ref            String? @db.VarChar(255)
  etapa              String? @db.VarChar(255)
  prazo_leitura      String? @db.VarChar(255)
  prazo_regulatorio  String? @db.VarChar(255)
  envio_releitura    String? @db.VarChar(255)
  prazo_releitura    String? @db.VarChar(255)
  envio_massiva      String? @db.VarChar(255)
  prazo_massiva      String? @db.VarChar(255)
  vencimento_fatura  String? @db.VarChar(255)
  envio_leitura      String? @db.VarChar(255)
  prazo_leitura_fimm String? @db.VarChar(255)
  id                 BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model cidades_localidades {
  regional String? @db.VarChar(255)
  cidade   String? @db.VarChar(255)
  distrito String? @db.VarChar(255)
  local    String? @db.VarChar(255)
  id       BigInt  @id @default(autoincrement())

  @@ignore
}

model clima_tempo {
  id                  Int       @id @default(autoincrement())
  livro               String
  data                DateTime  @db.Date
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 07:00-08:30 String? @map("07:00-08:30")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 08:30-10:00 String? @map("08:30-10:00")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 10:00-11:30 String? @map("10:00-11:30")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 11:30-13:00 String? @map("11:30-13:00")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 13:00-14:30 String? @map("13:00-14:30")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 14:30-16:00 String? @map("14:30-16:00")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 16:00-17:30 String? @map("16:00-17:30")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 17:30-18:30 String? @map("17:30-18:30")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  alerta_defesa_civil String?
  atualizado_em       DateTime? @default(now()) @db.Timestamptz(6)

  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 00:00-03:00 String? @map("00:00-03:00")
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 03:00-06:00 String? @map("03:00-06:00")
  @@unique([livro, data])
}

model contr_execucao_leitura {
  etapa                       String? @db.VarChar(255)
  tipo_oss                    String? @db.VarChar(255)
  subtipo_os                  String? @db.VarChar(255)
  numero_os                   String? @db.VarChar(255)
  localidade                  String? @db.VarChar(255)
  livro                       String? @db.VarChar(255)
  empreiteira                 String? @db.VarChar(255)
  data_recebimento            String? @db.VarChar(255)
  hora_recebimento            String? @db.VarChar(255)
  data_prevista_limite        String? @db.VarChar(255)
  data_ultima_atualizacao     String? @db.VarChar(255)
  qtd_digitados_nao_digitados String? @db.VarChar(255)
  qtd_com_leitura_sem_leitura String? @db.VarChar(255)
  percentual_sem_leitura      String? @db.VarChar(255)
  qtd_fora_de_faixa_foto      String? @db.VarChar(255)
  situacao                    String? @db.VarChar(255)
  data_import                 String? @db.VarChar(255)
  hora_import                 String? @db.VarChar(255)
  id                          Int     @id @default(autoincrement())
}

model control_empreiteiras {
  concessionaria                 String? @db.VarChar(255)
  empreiteira                    String? @db.VarChar(255)
  equipe                         String? @db.VarChar(255)
  nome_do_usuario                String? @db.VarChar(255)
  mes_ref_livro                  String? @db.VarChar(255)
  data_da_leitura                String? @db.VarChar(255)
  hora_da_leitura                String? @db.VarChar(255)
  unidade_consumidora            String? @db.VarChar(255)
  codigo_da_localidade           String? @db.VarChar(255)
  descricao_da_localidade        String? @db.VarChar(255)
  tipo_de_localizacao_da_uc      String? @db.VarChar(255)
  etapa                          String? @db.VarChar(255)
  livro                          String? @db.VarChar(255)
  status_releitura               String? @db.VarChar(255)
  equipamento                    String? @db.VarChar(255)
  especificacao                  String? @db.VarChar(255)
  mensagem                       String?
  mensagem_auxiliar              String?
  observacao_de_campo            String?
  status_foto                    String? @db.VarChar(255)
  faturamento_em_campo           String? @db.VarChar(255)
  status_impressao_do_comunicado String? @db.VarChar(255)
  forma_de_entrega               String? @db.VarChar(255)
  id                             BigInt  @id @default(autoincrement())

  @@index([livro], map: "idx_control_empreiteiras_livro")
  @@index([mes_ref_livro], map: "idx_control_empreiteiras_mes")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model coordenadas_municipios {
  Municipio String? @db.VarChar(255)
  lat       String? @db.VarChar(255)
  long      String? @db.VarChar(255)
  id        Int     @id @default(autoincrement())
  estado    String? @db.VarChar(255)

  @@ignore
}

model coordenadas_uc_livro {
  id                    Int      @id @default(autoincrement())
  livro                 String?  @db.VarChar(50)
  total_unidades        Int?
  unidades_consumidoras String[]
  coordenadas           Json?
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model cortes {
  mes_ref        String? @db.VarChar(255)
  municipio      String? @db.VarChar(255)
  etapa          String? @db.VarChar(255)
  livro          String? @db.VarChar(255)
  cod_leiturista String? @db.VarChar(255)
  leiturista     String? @db.VarChar(255)
  ocor_desc      String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model dados {
  data_leitura String? @db.VarChar(255)
  regional     String? @db.VarChar(255)
  municipio    String? @db.VarChar(255)
  livro        String? @db.VarChar(255)
  contagem_UC  String? @db.VarChar(255)
  etapa        String? @db.VarChar(255)
  nome_usuario String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model descontos_colaboradores {
  mes_ref           String? @db.VarChar(255)
  colaborador       String? @db.VarChar(255)
  categoria         String? @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 202 String? @map("202") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 226 String? @map("226") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 230 String? @map("230") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 232 String? @map("232") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 234 String? @map("234") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 240 String? @map("240") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 242 String? @map("242") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 244 String? @map("244") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 250 String? @map("250") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 260 String? @map("260") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 270 String? @map("270") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 275 String? @map("275") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 450 String? @map("450") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  total_descontado  String? @db.VarChar(255)
  qtd__230          String? @map("qtd. 230") @db.VarChar(255)
  qtd__232          String? @map("qtd. 232") @db.VarChar(255)
  qtd__234          String? @map("qtd. 234") @db.VarChar(255)
  qtd__240          String? @map("qtd. 240") @db.VarChar(255)
  qtd__242          String? @map("qtd. 242") @db.VarChar(255)
  qtd__244          String? @map("qtd. 244") @db.VarChar(255)
  qtd__250          String? @map("qtd. 250") @db.VarChar(255)
  qtd__260          String? @map("qtd. 260") @db.VarChar(255)
  qtd__270          String? @map("qtd. 270") @db.VarChar(255)
  qtd__275          String? @map("qtd. 275") @db.VarChar(255)
  qtd__450          String? @map("qtd. 450") @db.VarChar(255)
  qtd__202          String? @map("qtd. 202") @db.VarChar(255)
  qtd__226          String? @map("qtd. 226") @db.VarChar(255)
  regional          String? @db.VarChar(255)
  login             String? @db.VarChar(255)
  lancamento_manual String? @db.VarChar(255)

  @@ignore
}

model em_execucao_im {
  tipo_ss                     String?
  subtipo_os                  String?
  mr                          String?
  numero_os                   String?
  los                         String?
  local                       String?
  livro                       String?
  etapa                       String?
  dt_rec_abertura             String?
  dt_prev_limite              String?
  numero_solicitacao          String?
  uc                          String?
  bairro                      String?
  releitura                   String?
  qtd_digitados_nao_digitados String?
  leiturista                  String?
  dt_import                   String?
  hr_import                   String?
  mes_ref                     String?
  id                          BigInt  @id @default(autoincrement())
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model endereco_leituristas {
  matricula  String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  cargo      String? @db.VarChar(255)
  regional   String? @db.VarChar(255)
  cidade     String? @db.VarChar(255)
  id         BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model erros {
  cod_loc         String? @db.VarChar(255)
  etapa           String? @db.VarChar(255)
  livro           String? @db.VarChar(255)
  uc              String? @db.VarChar(255)
  Tipo_erro       String? @map("Tipo erro") @db.VarChar(255)
  sub_tipo        String? @db.VarChar(255)
  Registro___nome String? @map("Registro - nome") @db.VarChar(255)
  dt_lt_ant       String? @db.VarChar(255)
  lt_ant          String? @db.VarChar(255)
  dt_leit         String? @db.VarChar(255)
  leitura         String? @db.VarChar(255)
  Mens_Lt_        String? @map("Mens_Lt.") @db.VarChar(255)
  Cons_Leit_      String? @map("Cons_Leit.") @db.VarChar(255)
  Dt_Rel_         String? @map("Dt_Rel.") @db.VarChar(255)
  Lt_Rel_         String? @map("Lt_Rel.") @db.VarChar(255)
  Mens_Rel_       String? @map("Mens_Rel.") @db.VarChar(255)
  Lt_Fat_         String? @map("Lt_Fat.") @db.VarChar(255)
  cons_fat        String? @db.VarChar(255)
  medidor         String? @db.VarChar(255)
  tipo_medidor    String? @db.VarChar(255)
  municipio       String? @db.VarChar(255)
  endereco        String? @db.VarChar(255)
  mes_ref         String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model faltas_suspensao {
  mes_ref       String? @db.VarChar(255)
  matricula     String? @db.VarChar(255)
  colaborador   String? @db.VarChar(255)
  cargo         String? @db.VarChar(255)
  base          String? @db.VarChar(255)
  data_falta    String? @db.VarChar(255)
  justificativa String? @db.VarChar(255)
  observacao    String? @db.VarChar(255)
  id            BigInt  @id @default(autoincrement())

  @@ignore
}

model ferias {
  mes_ref     String? @db.VarChar(255)
  matricula   String? @db.VarChar(255)
  colaborador String? @db.VarChar(255)
  base        String? @db.VarChar(255)
  abono       String? @db.VarChar(255)
  data_inicio String? @db.VarChar(255)
  data_fim    String? @db.VarChar(255)
  id          Int     @id @default(autoincrement())
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model folgas {
  mes_ref     String? @db.VarChar(255)
  matricula   String? @db.VarChar(255)
  colaborador String? @db.VarChar(255)
  cargo       String? @db.VarChar(255)
  base        String? @db.VarChar(255)
  data_folga  String? @db.VarChar(255)
  motivo      String? @db.VarChar(255)
  id          BigInt  @id @default(autoincrement())

  @@ignore
}

model historico_chuvas {
  id            Int       @id @default(autoincrement())
  municipio_id  Int
  municipio     String?   @db.VarChar(255)
  precipitacao  Float?
  temperatura   Float?
  hora          Int?
  data_registro DateTime? @default(dbgenerated("CURRENT_DATE")) @db.Date
  timestamp     DateTime? @default(now()) @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model impedimentos {
  mes_ref_livro   String? @db.VarChar(255)
  nome_do_usuario String? @db.VarChar(255)
  etapa           String? @db.VarChar(255)
  livro           String? @db.VarChar(255)
  mensagem        String? @db.VarChar(255)
  responsavel     String? @db.VarChar(255)
  origem_dados    String? @db.VarChar(255)
  local           String? @db.VarChar(255)
  quantidade      String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model impedimentos_sucessivos {
  mes_ref          String? @db.VarChar(255)
  meses            String? @db.VarChar(255)
  contrato         String? @db.VarChar(255)
  regional         String? @db.VarChar(255)
  municipio        String? @db.VarChar(255)
  fase_smart       String? @db.VarChar(255)
  local            String? @db.VarChar(255)
  etapa            String? @db.VarChar(255)
  livro            String? @db.VarChar(255)
  uc               String? @db.VarChar(255)
  uc_aneel         String? @db.VarChar(255)
  tipo_imp_        String? @map("tipo_imp.") @db.VarChar(255)
  cod_fat          String? @db.VarChar(255)
  leiturista       String? @db.VarChar(255)
  dta_lt           String? @db.VarChar(255)
  lt               String? @db.VarChar(255)
  orig_lt          String? @db.VarChar(255)
  msg_lt           String? @db.VarChar(255)
  texto_lt         String? @db.VarChar(255)
  foto_lt          String? @db.VarChar(255)
  msg_aux          String? @db.VarChar(255)
  dta_rlt          String? @db.VarChar(255)
  msg_rlt          String? @db.VarChar(255)
  rlt              String? @db.VarChar(255)
  texto_rlt        String? @db.VarChar(255)
  foto_rlt         String? @db.VarChar(255)
  tipo_medicao     String? @db.VarChar(255)
  tipo_md          String? @db.VarChar(255)
  nio              String? @db.VarChar(255)
  cste             String? @db.VarChar(255)
  nome_cliente_fat String? @db.VarChar(255)
  endereco         String? @db.VarChar(255)
  dta_sit_atual    String? @db.VarChar(255)
  dta_sit_vir_mes  String? @db.VarChar(255)
  dta_situ_uc_fat  String? @db.VarChar(255)
  pri_mes          String? @db.VarChar(255)
  sit_atual        String? @db.VarChar(255)
  sit_uc_fat       String? @db.VarChar(255)
  sit_vir_mes      String? @db.VarChar(255)
  flag_msg_035     String? @db.VarChar(255)
  msg_p_uc         String? @db.VarChar(255)
  val__ind_        String? @map("val._ind.") @db.VarChar(255)
  cons_mes         String? @db.VarChar(255)
  media_ano        String? @db.VarChar(255)
  cons             String? @map(">_cons") @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model item_202_Rec_Consumidores {
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  sistema    String? @db.VarChar(255)
  ajustes    String? @db.VarChar(255)
  total      String? @db.VarChar(255)
  mes_ref    String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model item_226_massivas_fora_prazo {
  mes_ref    String? @db.VarChar(255)
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  atraso_uc  String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model item_450_codigo_indevido_IRIN {
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  sistema    String? @db.VarChar(255)
  ajustes    String? @db.VarChar(255)
  total      String? @db.VarChar(255)
  mes_ref    String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model item_80_massivas {
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  sistema    String? @db.VarChar(255)
  ajustes    String? @db.VarChar(255)
  total      String? @db.VarChar(255)
  mes_ref    String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model item_82_massivas {
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  sistema    String? @db.VarChar(255)
  ajustes    String? @db.VarChar(255)
  total      String? @db.VarChar(255)
  mes_ref    String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model item_84_massivas_hibridas {
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  sistema    String? @db.VarChar(255)
  ajustes    String? @db.VarChar(255)
  total      String? @db.VarChar(255)
  mes_ref    String? @db.VarChar(255)

  @@ignore
}

model item_BMD_multa_leiturista {
  item                    String? @db.VarChar(255)
  descricao               String? @db.VarChar(255)
  id                      BigInt  @id(map: "item_BMD_multa_pkey") @default(autoincrement())
  valor_unitario_atual    String? @db.VarChar(255)
  lancamento_manual       String? @db.VarChar(255)
  valor_unitario_anterior String? @db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model itens_pagos {
  item                 String? @db.VarChar(255)
  valor_unitario_atual String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model jornadas_trabalho {
  mes_ref_livro   String?                  @db.VarChar
  nome_do_usuario String?                  @db.VarChar
  data_da_leitura DateTime?                @db.Date
  hora_inicio     DateTime?                @db.Time(6)
  hora_fim        DateTime?                @db.Time(6)
  horas_totais    Unsupported("interval")?
  etapa           String?                  @db.VarChar

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model justificativas_bmd {
  Processo                 String? @db.VarChar(255)
  Mes_Ref                  String? @db.VarChar(255)
  Etapa                    String? @db.VarChar(255)
  Livro                    String? @db.VarChar(255)
  Leiturista               String? @db.VarChar(255)
  Dta_Envio_Livro          String? @db.VarChar(255)
  Dta_Prev_Livro           String? @db.VarChar(255)
  Dta_Exec_Uc              String? @db.VarChar(255)
  Dias_Atraso_Livro        String? @db.VarChar(255)
  Local                    String? @db.VarChar(255)
  Observacao_Ajuste_Previo String?
  dt_inclusao              String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model leituristas_localidades {
  matricula       String? @db.VarChar(255)
  nome_leiturista String? @db.VarChar(255)
  local           String? @db.VarChar(255)
  cidade          String? @db.VarChar(255)
  perfil          String? @db.VarChar(255)
  status          String? @db.VarChar(255)
  regional        String? @db.VarChar(255)
  id              BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model lista_codigos_op {
  codigo      String? @db.VarChar(255)
  descricao   String? @db.VarChar(255)
  responsavel String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model massivas {
  leiturista      String? @db.VarChar(255)
  status          String? @db.VarChar(255)
  etapa           String? @db.VarChar(255)
  livro           String? @db.VarChar(255)
  local           String? @db.VarChar(255)
  dt_rec_abertura String? @db.VarChar(255)
  dt_prev_limite  String? @db.VarChar(255)
  dt_import       String? @db.VarChar(255)
  hr_import       String? @db.VarChar(255)
  mes_ref         String? @db.VarChar(255)
  id              BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model medias_exec_livro {
  livro              String? @db.VarChar(255)
  meses_ativos       String? @db.VarChar(255)
  outliers_removidos String? @db.VarChar(255)
  ago_25             String? @db.VarChar(255)
  set_25             String? @db.VarChar(255)
  out_25             String? @db.VarChar(255)
  nov_25             String? @db.VarChar(255)
  dez_25             String? @db.VarChar(255)
  jan_26             String? @db.VarChar(255)
  fev_26             String? @db.VarChar(255)
  mar_26             String? @db.VarChar(255)
  abr_26             String? @db.VarChar(255)
  media_simples      String? @db.VarChar(255)
  mediana            String? @db.VarChar(255)
  desvio_padrao      String? @db.VarChar(255)
  media_robusta      String? @db.VarChar(255)
  tipo               String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model multas_BMD_evolucao {
  mes_ref                   String? @db.VarChar(255)
  categoria                 String? @db.VarChar(255)
  item                      String? @db.VarChar(255)
  descricao                 String? @db.VarChar(255)
  total_multa_inicial       String? @db.VarChar(255)
  total_multa_recurso       String? @db.VarChar(255)
  total_multa_contrarecurso String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model multas_BMD_regional {
  mes_ref                   String? @db.VarChar(255)
  regional                  String? @db.VarChar(255)
  categoria                 String? @db.VarChar(255)
  item                      String? @db.VarChar(255)
  descricao                 String? @db.VarChar(255)
  total_multa_inicial       String? @db.VarChar(255)
  total_multa_recurso       String? @db.VarChar(255)
  total_multa_contrarecurso String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model multas_lancamento_manual {
  mes_ref    String? @db.VarChar(255)
  item       String? @db.VarChar(255)
  local      String? @db.VarChar(255)
  etapa      String? @db.VarChar(255)
  livro      String? @db.VarChar(255)
  leiturista String? @db.VarChar(255)
  total      String? @db.VarChar(255)

  @@ignore
}

model pendentes_im {
  tipo_ss                     String?
  subtipo_os                  String?
  mr                          String?
  numero_os                   String?
  los                         String?
  local                       String?
  livro                       String?
  etapa                       String?
  dt_rec_abertura             String?
  dt_prev_limite              String?
  numero_solicitacao          String?
  uc                          String?
  bairro                      String?
  releitura                   String?
  qtd_digitados_nao_digitados String?
  dt_import                   String?
  hr_import                   String?
  mes_ref                     String?
  id                          BigInt  @id @default(autoincrement())
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model pix {
  data_servico   String? @db.VarChar(255)
  nome_usuario   String? @db.VarChar(255)
  cpf            String? @db.VarChar(255)
  diaria         String? @db.VarChar(255)
  tipo           String? @db.VarChar(255)
  total_leituras String? @db.VarChar(255)
  valor_receber  String? @db.VarChar(255)
  chave_PIX      String? @db.VarChar(255)
  obs            String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model plan_justificativas_coordenador {
  mes_ref          String? @db.VarChar(255)
  regional         String? @db.VarChar(255)
  data_da_inclusao String? @db.VarChar(255)
  etapa            String? @db.VarChar(255)
  livro            String? @db.VarChar(255)
  releitura        String? @db.VarChar(255)
  justificativa    String?
  link             String? @db.VarChar(255)
  dt_inclusao      String? @db.VarChar(255)

  @@ignore
}

model pontos_grade {
  id           Int    @id @default(autoincrement())
  municipio_id Int
  municipio    String @db.VarChar(255)
  direcao      String @db.VarChar(10)
  lat          BigInt
  long         BigInt

  @@index([municipio_id], map: "idx_pontos_grade_municipio")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model produtividade {
  mes_ref           String? @db.VarChar(255)
  local             String? @db.VarChar(255)
  etapa             String? @db.VarChar(255)
  livro             String? @db.VarChar(255)
  cod_leiturista    String? @db.VarChar(255)
  leiturista        String? @db.VarChar(255)
  total             String? @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 0 String? @map("0") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  // 99 String? @map("99") @db.VarChar(255)
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  /// This field was commented out because of an invalid name. Please provide a valid one that matches [a-zA-Z][a-zA-Z0-9_]*
  total_impedimento String? @db.VarChar(255)
  retornado_lis     String? @db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model qualidade {
  mes_ref        String? @db.VarChar(255)
  local          String? @db.VarChar(255)
  etapa          String? @db.VarChar(255)
  livro          String? @db.VarChar(255)
  especificacao  String? @db.VarChar(255)
  item           String? @db.VarChar(255)
  valor_estimado String? @db.VarChar(255)
  cod_leiturista String? @db.VarChar(255)
  leiturista     String? @db.VarChar(255)

  @@ignore
}

model relatorio_ativos_produtividade {
  id                      Int       @id @default(autoincrement())
  mes_referencia          DateTime? @db.Date
  situacao                String?   @db.VarChar(255)
  matricula               String?   @db.VarChar(255)
  colaborador             String?   @db.VarChar(255)
  cargo                   String?   @db.VarChar(255)
  base                    String?   @db.VarChar(255)
  data_atualizacao_ativos DateTime? @db.Date
  dia_1                   Json?
  dia_2                   Json?
  dia_3                   Json?
  dia_4                   Json?
  dia_5                   Json?
  dia_6                   Json?
  dia_7                   Json?
  dia_8                   Json?
  dia_9                   Json?
  dia_10                  Json?
  dia_11                  Json?
  dia_12                  Json?
  dia_13                  Json?
  dia_14                  Json?
  dia_15                  Json?
  dia_16                  Json?
  dia_17                  Json?
  dia_18                  Json?
  dia_19                  Json?
  dia_20                  Json?
  dia_21                  Json?
  dia_22                  Json?
  dia_23                  Json?
  dia_24                  Json?
  dia_25                  Json?
  dia_26                  Json?
  dia_27                  Json?
  dia_28                  Json?
  dia_29                  Json?
  dia_30                  Json?
  dia_31                  Json?
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rota_seq {
  regiao    String? @db.VarChar(255)
  local     String? @db.VarChar(255)
  municipio String? @db.VarChar(255)
  etapa     String? @db.VarChar(255)
  livro     String? @db.VarChar(255)
  seq       String? @db.VarChar(255)
  uc        String? @db.VarChar(255)
  sit       String? @db.VarChar(255)
  lat       String? @db.VarChar(255)
  long      String? @db.VarChar(255)
  id        BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model servicos_manutencao {
  centro_de_custo String? @db.VarChar(255)
  veiculo         String? @db.VarChar(255)
  colaborador     String? @db.VarChar(255)
  base            String? @db.VarChar(255)
  descricao_item  String?
  ordem_servico   String? @db.VarChar(255)
  data_servico    String? @db.VarChar(255)
  hodometro       String? @db.VarChar(255)
  mes_ref         String? @db.VarChar(255)

  @@ignore
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model spatial_ref_sys {
  srid      Int     @id
  auth_name String? @db.VarChar(256)
  auth_srid Int?
  srtext    String? @db.VarChar(2048)
  proj4text String? @db.VarChar(2048)
}

model sumarizacao_leiturista {
  mes_referencia                                                  String? @db.VarChar(255)
  regional                                                        String? @db.VarChar(255)
  empresa                                                         String? @db.VarChar(255)
  contrato                                                        String? @db.VarChar(255)
  registro_leiturista                                             String? @db.VarChar(255)
  nome_leiturista                                                 String? @db.VarChar(255)
  valor_medicao                                                   String? @db.VarChar(255)
  valor_multa                                                     String? @db.VarChar(255)
  porcentagem_multa                                               String? @db.VarChar(255)
  total_de_leituras                                               String? @map("2_total_de_leituras") @db.VarChar(255)
  leituras_enviadas_para_execucao                                 String? @map("4_leituras_enviadas_para_execucao") @db.VarChar(255)
  leituras_enviadas_para_execucao_urbana                          String? @map("6_leituras_enviadas_para_execucao_urbana") @db.VarChar(255)
  leituras_enviadas_para_execucao_rural_plurimensal               String? @map("8_leituras_enviadas_para_execucao_rural_plurimensal") @db.VarChar(255)
  leituras_enviadas_para_execucao_rural_avulsa                    String? @map("10_leituras_enviadas_para_execucao_rural_avulsa") @db.VarChar(255)
  leituras_retornadas_do_campo_urbano                             String? @map("12_leituras_retornadas_do_campo_urbano") @db.VarChar(255)
  leituras_retornadas_do_campo_rural_plurimensal                  String? @map("14_leituras_retornadas_do_campo_rural_plurimensal") @db.VarChar(255)
  leituras_retornadas_do_campo_rural_avulsa                       String? @map("16_leituras_retornadas_do_campo_rural_avulsa") @db.VarChar(255)
  leituras_retornadas_sem_foto_rural_plurimensal                  String? @map("17_leituras_retornadas_sem_foto_rural_plurimensal") @db.VarChar(255)
  leituras_retornadas_sem_foto_rural_avulsa                       String? @map("18_leituras_retornadas_sem_foto_rural_avulsa") @db.VarChar(255)
  total_de_faturas_enviadas_para_impressao                        String? @map("19_total_de_faturas_enviadas_para_impressao") @db.VarChar(255)
  total_faturado_em_campo                                         String? @map("20_total_faturado_em_campo") @db.VarChar(255)
  leitura_e_impressao_executada_em_campo                          String? @map("21_leitura_e_impressao_executada_em_campo") @db.VarChar(255)
  sem_leitura_e_impressao_executada_em_campo                      String? @map("22_sem_leitura_e_impressao_executada_em_campo") @db.VarChar(255)
  leituras_sem_impressao_bloqueio_copel                           String? @map("24_leituras_sem_impressao_bloqueio_copel") @db.VarChar(255)
  leitura_sem_impressao_bloqueio_executor_leiturista              String? @map("26_leitura_sem_impressao_bloqueio_executor_leiturista") @db.VarChar(255)
  sem_leitura_e_bloqueio_de_imprssao_em_campo                     String? @map("28_sem_leitura_e_bloqueio_de_imprssao_em_campo") @db.VarChar(255)
  indicacao_codigo_irregularidade_sem_respectiva_leitura          String? @map("30_indicacao_codigo_irregularidade_sem_respectiva_leitura") @db.VarChar(255)
  indicacao_codigo_irregularidade_20_ou_25                        String? @map("32_indicacao_codigo_irregularidade_20_ou_25") @db.VarChar(255)
  indicacao_codigo_irregularidade_30                              String? @map("34_indicacao_codigo_irregularidade_30") @db.VarChar(255)
  indicacao_codigo_irregularidade_3_11_21_24_26_31_53_63_64       String? @map("36_indicacao_codigo_irregularidade_3_11_21_24_26_31_53_63_64") @db.VarChar(255)
  indicacao_cod_ref_prob_estruturais_na_uc_40_41_42_43_44_45_4    String? @map("38_indicacao_cod_ref_prob_estruturais_na_uc_40_41_42_43_44_45_4") @db.VarChar(255)
  fotografias_executadas_urbana                                   String? @map("48_fotografias_executadas_urbana") @db.VarChar(255)
  confirmacao_de_leitura_releitura_enviadas                       String? @map("50_confirmacao_de_leitura_releitura_enviadas") @db.VarChar(255)
  releituras_enviadas_urbano                                      String? @map("52_releituras_enviadas_urbano") @db.VarChar(255)
  releituras_enviadas_rural                                       String? @map("54_releituras_enviadas_rural") @db.VarChar(255)
  releituras_retornadas_sem_foto_urbana                           String? @map("56_releituras_retornadas_sem_foto_urbana") @db.VarChar(255)
  releituras_retornadas_com_foto_urbana                           String? @map("58_releituras_retornadas_com_foto_urbana") @db.VarChar(255)
  releituras_retornadas_com_foto_rural                            String? @map("60_releituras_retornadas_com_foto_rural") @db.VarChar(255)
  releituras_retornadas_sem_foto_rural                            String? @map("62_releituras_retornadas_sem_foto_rural") @db.VarChar(255)
  releitura_sem_leitura_so_com_irregularidade_urbana              String? @map("64_releitura_sem_leitura_so_com_irregularidade_urbana") @db.VarChar(255)
  releitura_sem_leitura_so_com_irregularidade_rural               String? @map("66_releitura_sem_leitura_so_com_irregularidade_rural") @db.VarChar(255)
  entrega_aviso_avulso_urbano                                     String? @map("70_entrega_aviso_avulso_urbano") @db.VarChar(255)
  entrega_aviso_avulso_rural                                      String? @map("72_entrega_aviso_avulso_rural") @db.VarChar(255)
  inclusao_de_nio_nao_constante_no_cadastro                       String? @map("74_inclusao_de_nio_nao_constante_no_cadastro") @db.VarChar(255)
  execucao_de_atualizacao_cadastral                               String? @map("76_execucao_de_atualizacao_cadastral") @db.VarChar(255)
  servicos_adicionais_leiturista__atual_cad_                      String? @map("78_servicos_adicionais_leiturista_(atual_cad)") @db.VarChar(255)
  impressao_massiva_no_local_da_uc                                String? @map("80_impressao_massiva_no_local_da_uc") @db.VarChar(255)
  impressao_massiva_no_local_da_uc_livro_cheio___35_              String? @map("82_impressao_massiva_no_local_da_uc_livro_cheio_>_35%") @db.VarChar(255)
  impressao_massiva_livro_hibrido                                 String? @map("84_impressao_massiva_livro_hibrido") @db.VarChar(255)
  impressao_massiva_livro_hibrido__35_                            String? @map("85_impressao_massiva_livro_hibrido_>35%") @db.VarChar(255)
  impressao_massiva_nao_executada_em_livro_hibridro               String? @map("86_impressao_massiva_nao_executada_em_livro_hibridro") @db.VarChar(255)
  antigo_item_corte_mob_e_ds                                      String? @map("88_antigo_item_corte_mob_e_ds") @db.VarChar(255)
  corte_gaviao                                                    String? @map("90_corte_gaviao") @db.VarChar(255)
  desligamento_virtual                                            String? @map("91_desligamento_virtual") @db.VarChar(255)
  leituras_obrigatorias_enviadas_urbano                           String? @map("92_leituras_obrigatorias_enviadas_urbano") @db.VarChar(255)
  leituras_obrigatorias_enviadas_rural_plurimensal                String? @map("94_leituras_obrigatorias_enviadas_rural_plurimensal") @db.VarChar(255)
  total_de_ucs_com_mdm                                            String? @map("100_total_de_ucs_com_mdm") @db.VarChar(255)
  total_de_ucs_com_mdm_urbano                                     String? @map("102_total_de_ucs_com_mdm_urbano") @db.VarChar(255)
  total_de_ucs_com_mdm_rural                                      String? @map("104_total_de_ucs_com_mdm_rural") @db.VarChar(255)
  total_de_leituras_recebidas_mdm_urbano                          String? @map("106_total_de_leituras_recebidas_mdm_urbano") @db.VarChar(255)
  total_de_leituras_recebidas_mdm_rural                           String? @map("108_total_de_leituras_recebidas_mdm_rural") @db.VarChar(255)
  treinamento_fbleit                                              String? @map("110_treinamento_fbleit") @db.VarChar(255)
  treinamento_fbleit_rec                                          String? @map("112_treinamento_fbleit_rec") @db.VarChar(255)
  treinamento_pilotagem_segura_de_motociclista                    String? @map("114_treinamento_pilotagem_segura_de_motociclista") @db.VarChar(255)
  pagamento_servicos_porta_a_porta                                String? @map("149_pagamento_servicos_porta_a_porta") @db.VarChar(255)
  pagamentos_servicos_de_eletricista                              String? @map("150_pagamentos_servicos_de_eletricista") @db.VarChar(255)
  servicos_combate_inadimplencia_                                 String? @map("151_servicos_combate_inadimplencia_") @db.VarChar(255)
  impressao_massiva_hibrido___20___excluir_                       String? @map("180_impressao_massiva_hibrido_<_20%_(excluir)") @db.VarChar(255)
  impressao_massiva_hibrido___20_                                 String? @map("184_impressao_massiva_hibrido_<_20%") @db.VarChar(255)
  impressao_massiva_hibrido___20__e___40_                         String? @map("185_impressao_massiva_hibrido_>_20%_e_<_40%") @db.VarChar(255)
  impressao_massiva_hibrido___40__e___60_                         String? @map("186_impressao_massiva_hibrido_>_40%_e_<_60%") @db.VarChar(255)
  impressao_massiva_hibrido___60_                                 String? @map("187_impressao_massiva_hibrido_>_60%") @db.VarChar(255)
  outros_pagamentos                                               String? @map("199_outros_pagamentos") @db.VarChar(255)
  nao_utilizacao_uniforme_epi_e_cracha                            String? @map("200_nao_utilizacao_uniforme_epi_e_cracha") @db.VarChar(255)
  reclamacao_de_consumidores                                      String? @map("202_reclamacao_de_consumidores") @db.VarChar(255)
  releituras_enviadas_e_nao_realizadas                            String? @map("209_releituras_enviadas_e_nao_realizadas") @db.VarChar(255)
  leituras_enviadas_e_nao_realizadas                              String? @map("210_leituras_enviadas_e_nao_realizadas") @db.VarChar(255)
  cobranca_atraso_manual__por_dia_                                String? @map("211_cobranca_atraso_manual_(por_dia)") @db.VarChar(255)
  atraso_ou_antecipacao_de_1_dia_na_execucao_da_leitura           String? @map("212_atraso_ou_antecipacao_de_1_dia_na_execucao_da_leitura") @db.VarChar(255)
  atraso_ou_antecipacao_de_2_dias_na_execucao_da_leitura          String? @map("213_atraso_ou_antecipacao_de_2_dias_na_execucao_da_leitura") @db.VarChar(255)
  atraso_ou_antecipacao_de_3_dias_na_execucao_da_leitura          String? @map("214_atraso_ou_antecipacao_de_3_dias_na_execucao_da_leitura") @db.VarChar(255)
  atraso_ou_antecipacao_de_4_ou_mais_dias_na_execucao_da_leit     String? @map("215_atraso_ou_antecipacao_de_4_ou_mais_dias_na_execucao_da_leit") @db.VarChar(255)
  atraso_de_1_dia_na_execucao_da_releitura                        String? @map("216_atraso_de_1_dia_na_execucao_da_releitura") @db.VarChar(255)
  atraso_de_2_dias_na_execucao_da_releitura                       String? @map("217_atraso_de_2_dias_na_execucao_da_releitura") @db.VarChar(255)
  atraso_de_3_ou_mais_dias_na_execucao_da_releitura               String? @map("218_atraso_de_3_ou_mais_dias_na_execucao_da_releitura") @db.VarChar(255)
  leitura_executada_sem_impressao_a_revelia                       String? @map("224_leitura_executada_sem_impressao_a_revelia") @db.VarChar(255)
  nao_entrega_de_fatura_massiva_no_prazo                          String? @map("226_nao_entrega_de_fatura_massiva_no_prazo") @db.VarChar(255)
  leitura_executado_entregue_rol_de_leituras                      String? @map("228_leitura_executado/entregue_rol_de_leituras") @db.VarChar(255)
  ajuste_por_erro_de_leitura_s_cod                                String? @map("230_ajuste_por_erro_de_leitura_s/cod") @db.VarChar(255)
  ajuste_por_erro_de_leitura_c_cod_05_95_99                       String? @map("232_ajuste_por_erro_de_leitura_c/cod_05_95_99") @db.VarChar(255)
  ajuste_devido_a_erro_leitura_na_releitura                       String? @map("234_ajuste_devido_a_erro_leitura_na_releitura") @db.VarChar(255)
  manutencao_por_erro_de_leitura_s_cod                            String? @map("240_manutencao_por_erro_de_leitura_s/cod") @db.VarChar(255)
  manutencao_por_erro_de_leitura_c_cod_05_95_99                   String? @map("242_manutencao_por_erro_de_leitura_c/cod_05_95_99") @db.VarChar(255)
  manutencao_devido_a_erro_leitura_na_releitura                   String? @map("244_manutencao_devido_a_erro_leitura_na_releitura") @db.VarChar(255)
  codigo_irregapontado_indevidamente_ou_irregular_ou_nao_se_a_250 String? @map("250_codigo_irregapontado_indevidamente_ou_irregular_ou_nao_se_a") @db.VarChar(255)
  erro_de_leitura_verificado_na_critica                           String? @map("260_erro_de_leitura_verificado_na_critica") @db.VarChar(255)
  erro_de_leitura_fatura_retida_antes_da_apresentacao             String? @map("262_erro_de_leitura_fatura_retida_antes_da_apresentacao") @db.VarChar(255)
  fotografia_incorreta_sem_relacao_a_leitura_acesso               String? @map("270_fotografia_incorreta/sem_relacao_a_leitura/acesso") @db.VarChar(255)
  corte_gaviao_realizado_de_forma_irregular_ou_indevida           String? @map("275_corte_gaviao_realizado_de_forma_irregular_ou_indevida") @db.VarChar(255)
  gps_desligado                                                   String? @map("280_gps_desligado") @db.VarChar(255)
  empregado_sem_registro                                          String? @map("282_empregado_sem_registro") @db.VarChar(255)
  falta_de_comunicacao_de_acidente_de_trabalho                    String? @map("284_falta_de_comunicacao_de_acidente_de_trabalho") @db.VarChar(255)
  falta_de_quaisquer_dos_requisitos_minimos_de_mao_de_obra_eq     String? @map("286_falta_de_quaisquer_dos_requisitos_minimos_de_mao_de_obra_eq") @db.VarChar(255)
  nao_cumprimento_do_guia_saude_e_seguranca                       String? @map("288_nao_cumprimento_do_guia_saude_e_seguranca") @db.VarChar(255)
  falta_de_quaisquer_dos_requisitos_minimos_de_uniformes_e_ep     String? @map("289_falta_de_quaisquer_dos_requisitos_minimos_de_uniformes_e_ep") @db.VarChar(255)
  penalizacoes_conforme_contrato                                  String? @map("290_penalizacoes_conforme_contrato") @db.VarChar(255)
  bonificacao_primeira_medicao_aplicar_30__das_penalidades        String? @map("292_bonificacao_primeira_medicao_aplicar_30%_das_penalidades") @db.VarChar(255)
  bonificacao_segunda_medicao_aplicar_60__das_penalidades         String? @map("294_bonificacao_segunda_medicao_aplicar_60%_das_penalidades") @db.VarChar(255)
  bonificacao_indicador_de_qualidade_reduzir_30__das_penalida     String? @map("296_bonificacao_indicador_de_qualidade_reduzir_30%_das_penalida") @db.VarChar(255)
  bonificacao_indicador_de_qualidade_reduzir_50__das_penalida     String? @map("298_bonificacao_indicador_de_qualidade_reduzir_50%_das_penalida") @db.VarChar(255)
  outras_penalidades__1_us_                                       String? @map("300_outras_penalidades_(1_us)") @db.VarChar(255)
  penalidades_leves                                               String? @map("350_penalidades_leves") @db.VarChar(255)
  penalidades_medias                                              String? @map("352_penalidades_medias") @db.VarChar(255)
  penalidades_altas                                               String? @map("354_penalidades_altas") @db.VarChar(255)
  penalidades_graves                                              String? @map("356_penalidades_graves") @db.VarChar(255)
  penalidades_gravissimas                                         String? @map("358_penalidades_gravissimas") @db.VarChar(255)
  reclamacao_de_consumidores_por_livro                            String? @map("402_reclamacao_de_consumidores_por_livro") @db.VarChar(255)
  ajuste_por_erro_de_leitura_s_cod_por_livro                      String? @map("430_ajuste_por_erro_de_leitura_s/cod_por_livro") @db.VarChar(255)
  ajuste_por_erro_de_leitura_c_cod_05_95_99_por_livro             String? @map("432_ajuste_por_erro_de_leitura_c/cod_05_95_99_por_livro") @db.VarChar(255)
  ajuste_devido_a_erro_leitura_na_releitura_por_livro             String? @map("434_ajuste_devido_a_erro_leitura_na_releitura_por_livro") @db.VarChar(255)
  manutencao_por_erro_de_leitura_s_cod_por_livro                  String? @map("440_manutencao_por_erro_de_leitura_s/cod_por_livro") @db.VarChar(255)
  manutencao_por_erro_de_leitura_c_cod_05_95_99_por_livro         String? @map("442_manutencao_por_erro_de_leitura_c/cod_05_95_99_por_livro") @db.VarChar(255)
  manutencao_devido_a_erro_leitura_na_releitura_por_livro         String? @map("444_manutencao_devido_a_erro_leitura_na_releitura_por_livro") @db.VarChar(255)
  codigo_irregapontado_indevidamente_ou_irregular_ou_nao_se_a_450 String? @map("450_codigo_irregapontado_indevidamente_ou_irregular_ou_nao_se_a") @db.VarChar(255)
  erro_de_leitura_verificado_na_critica_por_livro                 String? @map("460_erro_de_leitura_verificado_na_critica_por_livro") @db.VarChar(255)
  erro_de_leitura_fatura_retida_antes_da_apresentacao_por_liv     String? @map("462_erro_de_leitura_fatura_retida_antes_da_apresentacao_por_liv") @db.VarChar(255)
  fotografia_incorreta_sem_relacao_a_leitura_acesso_por_livro     String? @map("470_fotografia_incorreta/sem_relacao_a_leitura/acesso_por_livro") @db.VarChar(255)
  corte_gaviao_realizado_de_forma_irregular_ou_indevida_por_l     String? @map("475_corte_gaviao_realizado_de_forma_irregular_ou_indevida_por_l") @db.VarChar(255)
  total_de_vistorias                                              String? @map("500_total_de_vistorias") @db.VarChar(255)
  id                                                              BigInt  @id @default(autoincrement())
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model supervisor_regional {
  local        String? @db.VarChar(255)
  municipio    String? @db.VarChar(255)
  cod_regional String? @db.VarChar(255)
  regional     String? @db.VarChar(255)
  supervisor   String? @db.VarChar(255)
  id           BigInt  @id @default(autoincrement())

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model tab_ligacao_coordenadas {
  UC        String? @db.VarChar(255)
  latitude  String? @db.VarChar(255)
  longitude String? @db.VarChar(255)

  @@ignore
}

model users {
  id            Int      @id @default(autoincrement())
  nome          String   @db.VarChar(255)
  email         String   @unique @db.VarChar(255)
  senha         String   @db.VarChar(255)
  nivel         String   @default("USUARIO") @db.VarChar(20)
  ativo         Boolean  @default(true)
  criado_em     DateTime @default(now()) @db.Timestamp(6)
  atualizado_em DateTime @default(now()) @db.Timestamp(6)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model prazo_reg_livros {
  mes_ref                 String? @db.VarChar(255)
  regional                String? @db.VarChar(255)
  municipio               String? @db.VarChar(255)
  local                   String? @db.VarChar(255)
  etapa                   String? @db.VarChar(255)
  livro                   String? @db.VarChar(255)
  ultimo_executor         String? @db.VarChar(255)
  primeira                String? @db.VarChar(255)
  ultima                  String? @db.VarChar(255)
  prazo_calendario        String? @db.VarChar(255)
  dias_iniciais           String? @db.VarChar(255)
  dias_finais             String? @db.VarChar(255)
  tempo_de_execucao       String? @db.VarChar(255)
  volume_de_leituras      String? @db.VarChar(255)
  id                      BigInt  @id @default(autoincrement())
  calendario_mes_seguinte String? @db.VarChar(255)

  @@ignore
}
